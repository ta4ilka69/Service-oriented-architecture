<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:ws="http://www.mulesoft.org/schema/mule/ws"
      xmlns:tls="http://www.mulesoft.org/schema/mule/tls"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/ws http://www.mulesoft.org/schema/mule/ws/current/mule-ws.xsd
        http://www.mulesoft.org/schema/mule/tls http://www.mulesoft.org/schema/mule/tls/current/mule-tls.xsd">

    <http:listener-config name="httpsListener" host="0.0.0.0" port="${rest.port}">
        <http:tls-context>
            <tls:key-store path="${mule.keystore.path}" password="${mule.keystore.pass}" type="PKCS12"/>
        </http:tls-context>
    </http:listener-config>

    <http:requester-config name="httpsRequester" protocol="HTTPS">
        <tls:context>
            <tls:trust-store path="${mule.truststore.path}" password="${mule.truststore.pass}" type="JKS"/>
        </tls:context>
    </http:requester-config>

    <ws:consumer-config name="musicSoap"
                        wsdlLocation="https://localhost:5252/MusicBandSoapService?wsdl"
                        service="MusicBandSoapService"
                        port="MusicBandPort"
                        address="https://localhost:5252/MusicBandSoapService">
        <ws:connection requesterConfig="httpsRequester"/>
    </ws:consumer-config>

    <flow name="get-music-bands">
        <http:listener config-ref="httpsListener" path="/music-bands" allowedMethods="GET"/>
        <set-payload><![CDATA[%dw 2.0
output application/java
var q = attributes.queryParams default {}
---
{
  sort: (q.sort default []) as Array,
  page: (q.page default null) as Number?,
  size: (q.size default null) as Number?,
  filter: (q.filter default []) as Array
}]]></set-payload>
        <ws:consume config-ref="musicSoap" operation="ListMusicBands"/>
    </flow>

</mule>


